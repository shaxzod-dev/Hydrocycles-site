// const NAME_REGEX = /^[a-zA-Z]+$/;

export const isValidName = (name) => name.trim().length >= 3;

// export const validateName = (str) => {
//   return str.trim().length >= 2;
// };

// export const validateLastName = (str) => {
//   return str.trim().length >= 2;
// };
